# cat=basic//01; type=string label=GNUPG Home (Default: $HOME/.gnupg) while $HOME is the home of the user the PHP-FPM or the Webserver runs as. This directory needs to exist and be writable for PHP
gnupghome =
